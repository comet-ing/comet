query vouchers {
  vouchers {
    edges {
      voucher: node {
        index
        input {
          id
          index
          payload
        }
        proof {
          outputIndex
          outputHashesSiblings
        }
        destination
        value
        payload
        executed
      }
    }
  }
}